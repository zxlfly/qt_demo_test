# 文本闪烁区域功能 — 技术文档

## 功能概述

在界面中提供一个可配置的网格区域，每个格子包含：
- **固定标签**：如 "a:"、"b:" 等，始终显示
- **闪烁文字**：黄色文字，按设定频率闪烁（显示/隐藏切换）

支持：
- 构造时指定行数和列数（如 2×2、3×2）
- 每个格子独立控制：可随时设置或清除闪烁文字
- 多个格子可同时闪烁
- 闪烁频率通过常量配置

---

## 架构设计

```
┌─────────────────────────────────────────────────┐
│              FlashTextManager                     │
│                                                   │
│  构造参数: rows, cols                             │
│                                                   │
│  ┌───────────────────────────────────────────┐   │
│  │              QGridLayout                   │   │
│  │                                            │   │
│  │  ┌─────────────┐  ┌─────────────┐        │   │
│  │  │ FlashCell    │  │ FlashCell    │  ...   │   │
│  │  │ [标签] [闪烁] │  │ [标签] [闪烁] │        │   │
│  │  │  (0,0)      │  │  (0,1)      │        │   │
│  │  └─────────────┘  └─────────────┘        │   │
│  │  ┌─────────────┐  ┌─────────────┐        │   │
│  │  │ FlashCell    │  │ FlashCell    │  ...   │   │
│  │  │ [标签] [闪烁] │  │ [标签] [闪烁] │        │   │
│  │  │  (1,0)      │  │  (1,1)      │        │   │
│  │  └─────────────┘  └─────────────┘        │   │
│  │          ...           ...                 │   │
│  └───────────────────────────────────────────┘   │
│                                                   │
│  控制接口:                                        │
│    setLabel(row, col, text)                       │
│    setFlashText(row, col, text)                   │
│    stopFlash(row, col)                            │
│    stopAll()                                      │
└─────────────────────────────────────────────────┘
```

### FlashCell 内部结构

```
┌─────────────────────────────────────────┐
│               FlashCell                  │
│                                          │
│  ┌──────────┐  ┌──────────┐  ┌───────┐ │
│  │  QLabel   │  │  QLabel   │  │ Stretch│ │
│  │ (固定标签) │  │ (闪烁文字) │  │       │ │
│  │  白色文字  │  │  黄色文字  │  │       │ │
│  └──────────┘  └──────────┘  └───────┘ │
│                                          │
│  QTimer: 按 FLASH_TOGGLE_INTERVAL_MS     │
│          切换闪烁文字的显示/隐藏           │
└─────────────────────────────────────────┘
```

### 核心流程

```
setFlashText(row, col, "警告信息")
  → 定位 FlashCell (通过 row * cols + col 计算索引)
  → 设置 QLabel 文字内容
  → 显示 QLabel
  → 启动 QTimer（若未运行）
  → 定时器触发：切换 visible (true ↔ false)
  → 闪烁效果：黄色文字显示 → 隐藏 → 显示 → ...

stopFlash(row, col)
  → 定位 FlashCell
  → 停止 QTimer
  → 隐藏 QLabel
  → 清空文字内容
```

---

## 文件清单

| 文件 | 说明 |
|------|------|
| `flashcell.h` | FlashCell 类声明（单个格子） |
| `flashcell.cpp` | FlashCell 实现（固定标签 + 闪烁文字 + 定时器） |
| `flashtextmanager.h` | FlashTextManager 类声明 + 闪烁间隔常量 |
| `flashtextmanager.cpp` | FlashTextManager 实现（网格布局 + 控制接口） |

### 依赖的 Qt 模块

- `QGridLayout`、`QHBoxLayout`、`QLabel`、`QTimer`

---

## 可调常量

```cpp
// 在 flashtextmanager.h 中定义
constexpr int FLASH_TOGGLE_INTERVAL_MS = 500;  // 闪烁切换间隔（毫秒）
```

---

## 接口说明

### FlashTextManager

```cpp
class FlashTextManager : public QWidget
{
    Q_OBJECT

public:
    // 构造时指定行数和列数
    explicit FlashTextManager(int rows, int cols, QWidget *parent = nullptr);

    // 设置某个格子的固定标签
    void setLabel(int row, int col, const QString &text);

    // 设置某个格子的闪烁文字（自动开始闪烁）
    void setFlashText(int row, int col, const QString &text);

    // 停止某个格子的闪烁（文字消失）
    void stopFlash(int row, int col);

    // 停止所有闪烁
    void stopAll();

    // 获取行数
    int rows() const;

    // 获取列数
    int cols() const;
};
```

### FlashCell

```cpp
class FlashCell : public QWidget
{
    Q_OBJECT

public:
    explicit FlashCell(QWidget *parent = nullptr);

    // 设置固定标签文字
    void setLabel(const QString &text);

    // 设置闪烁文字（自动开始闪烁）
    void setFlashText(const QString &text);

    // 停止闪烁（文字消失）
    void stopFlash();

    // 是否正在闪烁
    bool isFlashing() const;
};
```

> FlashCell 一般不需要直接使用，通过 FlashTextManager 的接口间接操作即可。

---

## 迁移步骤

### 1. 复制文件

将以下 4 个文件复制到目标项目中：
- `flashcell.h`
- `flashcell.cpp`
- `flashtextmanager.h`
- `flashtextmanager.cpp`

### 2. 添加到构建

在 CMakeLists.txt 的源文件列表中添加：
```cmake
flashcell.cpp
flashcell.h
flashtextmanager.cpp
flashtextmanager.h
```

### 3. 在页面中使用

```cpp
// MyPage.h
#include "flashtextmanager.h"

class MyPage : public QWidget
{
    Q_OBJECT
public:
    explicit MyPage(QWidget *parent = nullptr);

private:
    FlashTextManager *m_flashManager;
};

// MyPage.cpp
MyPage::MyPage(QWidget *parent)
    : QWidget(parent)
{
    auto *layout = new QVBoxLayout(this);

    // 创建两行两列的闪烁区域
    m_flashManager = new FlashTextManager(2, 2, this);
    layout->addWidget(m_flashManager);

    // 设置固定标签
    m_flashManager->setLabel(0, 0, "a:");
    m_flashManager->setLabel(0, 1, "b:");
    m_flashManager->setLabel(1, 0, "c:");
    m_flashManager->setLabel(1, 1, "d:");
}

// 在需要时设置闪烁文字
void MyPage::onAlertTriggered(int row, int col, const QString &message)
{
    m_flashManager->setFlashText(row, col, message);
}

// 在不需要时清除闪烁文字
void MyPage::onAlertCleared(int row, int col)
{
    m_flashManager->stopFlash(row, col);
}
```

### 4. 不同页面使用不同配置

```cpp
// 页面A：两行两列
auto *flashA = new FlashTextManager(2, 2, this);

// 页面B：三行两列
auto *flashB = new FlashTextManager(3, 2, this);

// 页面C：一行三列
auto *flashC = new FlashTextManager(1, 3, this);
```

---

## 显示样式

当前样式：
- **固定标签**：白色文字（`#FFFFFF`）
- **闪烁文字**：金黄色文字（`#FFD700`），按间隔显示/隐藏切换

如需修改颜色，在 `flashcell.cpp` 构造函数中修改：
```cpp
// 固定标签颜色
m_label->setStyleSheet("color: #FFFFFF;");

// 闪烁文字颜色
m_flashLabel->setStyleSheet("color: #FFD700;");
```

如需修改闪烁间隔，修改 `flashtextmanager.h` 中的常量：
```cpp
constexpr int FLASH_TOGGLE_INTERVAL_MS = 500;  // 毫秒
```

---

## 注意事项

1. **行列索引从 0 开始**：`setLabel(0, 0, ...)` 表示第 1 行第 1 列，越界访问会被静默忽略。
2. **闪烁文字替换**：对同一格子重复调用 `setFlashText()` 会更新文字内容，闪烁不会中断。
3. **停止闪烁**：调用 `stopFlash()` 后文字完全消失（隐藏 + 清空），固定标签不受影响。
4. **内存管理**：FlashTextManager 作为 QWidget 的子对象，父对象销毁时自动回收，无需手动 delete。
5. **闪烁定时器**：每个 FlashCell 独立持有 QTimer，格子之间互不影响。
6. **行数列数固定**：构造后行列数不可变更。如果需要动态调整，需要销毁重建 FlashTextManager。
