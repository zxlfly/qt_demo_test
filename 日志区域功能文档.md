# 日志区域功能 — 技术文档

## 功能概述

在界面中提供一个日志展示区域，支持：
- 日志信息逐条按时间间隔输出（如 1 秒一条）
- 每条日志自动附带输出时间戳（格式：`yyyy-MM-dd hh:mm:ss`）
- 每条日志输出后触发回调，供业务逻辑处理
- 日志量大时自动限制最大显示行数

---

## 架构设计

```
┌─────────────────────────────────────────────┐
│                LogManager                    │
│                                              │
│  ┌──────────┐    ┌─────────┐    ┌────────┐  │
│  │ LogEntry  │───▶│ QQueue  │───▶│ QTimer │  │
│  │ (日志对象) │    │ (缓冲队列)│    │(输出节奏)│  │
│  └──────────┘    └─────────┘    └────┬───┘  │
│                                       │       │
│                                       ▼       │
│                              ┌──────────────┐ │
│                              │  onTimeout()  │ │
│                              │  1. 出队一条   │ │
│                              │  2. 格式化显示 │ │
│                              │  3. emit 信号  │ │
│                              └──────┬───────┘ │
│                                     │         │
│                    ┌────────────────┘         │
│                    ▼                          │
│           signal: logProcessed(LogEntry)      │
└─────────────────────────────────────────────┘
```

### 核心流程

```
任意位置调用 appendLog(entry)
  → entry 入队 QQueue
  → QTimer 若未运行则启动
  → QTimer 触发 onTimeout()
    → dequeue 取出一条日志
    → 取当前时间格式化为 yyyy-MM-dd hh:mm:ss
    → 以 HTML 富文本追加到 QTextEdit（时间白色，内容黄色）
    → emit logProcessed(entry) → 外部槽函数处理业务
    → 队列为空则停止 QTimer
```

---

## 文件清单

| 文件 | 说明 |
|------|------|
| `logmanager.h` | LogEntry 结构体 + LogManager 类声明 |
| `logmanager.cpp` | 队列管理、定时器输出、信号触发 |

### 依赖的 Qt 模块

- `QTextEdit`（日志显示控件，需在 UI 或代码中创建）
- `QQueue`、`QTimer`、`QVariantMap`、`QDateTime`

---

## 数据结构

### LogEntry

```cpp
struct LogEntry {
    QString content;      // 日志内容
    QVariantMap extra;    // 其他业务数据（灵活扩展，键值对形式）
};
```

`extra` 字段用于携带附加数据，例如：
```cpp
entry.extra["type"] = "warning";
entry.extra["seq"] = 42;
entry.extra["source"] = "sensor_module";
```

### 可调常量

```cpp
constexpr int MAX_LOG_LINES = 1000;           // 最大显示行数
constexpr int LOG_OUTPUT_INTERVAL_MS = 1000;  // 日志输出间隔（毫秒）
```

---

## LogManager 公开接口

```cpp
class LogManager : public QObject
{
    Q_OBJECT

public:
    explicit LogManager(QObject *parent = nullptr);

    // 绑定日志显示控件（QTextEdit，只读模式）
    void setDisplay(QTextEdit *display);

    // 设置输出间隔（毫秒），可动态调整
    void setOutputInterval(int ms);

    // 追加一条日志到队列
    void appendLog(const LogEntry &entry);

    // 批量追加日志到队列
    void appendLogs(const QList<LogEntry> &entries);

signals:
    // 每条日志输出后发出，供外部执行业务逻辑
    void logProcessed(const LogEntry &entry);
};
```

---

## 迁移步骤

### 1. 复制文件

将 `logmanager.h` 和 `logmanager.cpp` 复制到目标项目中。

### 2. 添加到构建

在 CMakeLists.txt 的源文件列表中添加：
```cmake
logmanager.cpp
logmanager.h
```

### 3. 在 UI 中添加日志显示控件

在目标页面的 UI 文件中添加一个 `QTextEdit`，命名为 `logTextEdit`，设置为只读。

或者在代码中动态创建：
```cpp
QTextEdit *logTextEdit = new QTextEdit(this);
logTextEdit->setReadOnly(true);
```

### 4. 在页面类中初始化

```cpp
// MyPage.h
#include "logmanager.h"

class MyPage : public QWidget
{
    Q_OBJECT
public:
    explicit MyPage(QWidget *parent = nullptr);

private slots:
    void onLogProcessed(const LogEntry &entry);

private:
    LogManager *m_logManager;
};

// MyPage.cpp
MyPage::MyPage(QWidget *parent)
    : QWidget(parent)
    , m_logManager(new LogManager(this))
{
    // 绑定显示控件
    m_logManager->setDisplay(ui->logTextEdit);

    // 连接信号槽
    connect(m_logManager, &LogManager::logProcessed,
            this, &MyPage::onLogProcessed);
}

void MyPage::onLogProcessed(const LogEntry &entry)
{
    // 在此实现你的业务逻辑
    // entry.content - 日志内容
    // entry.extra   - 其他业务数据（QVariantMap）
}
```

### 5. 在任意位置输出日志

```cpp
// 单条日志
LogEntry entry;
entry.content = "设备连接成功";
entry.extra["type"] = "info";
entry.extra["deviceId"] = "sensor_01";
m_logManager->appendLog(entry);

// 批量日志
QList<LogEntry> entries;
for (int i = 0; i < 10; ++i) {
    LogEntry e;
    e.content = QString("批量日志 #%1").arg(i);
    entries.append(e);
}
m_logManager->appendLogs(entries);
```

---

## 显示样式

当前日志显示样式：
- **时间部分**：白色文字（`#FFFFFF`），等宽字体（Courier New）
- **内容部分**：金黄色文字（`#FFD700`）

如需修改颜色，在 `logmanager.cpp` 的 `onTimeout()` 方法中修改 HTML 模板：
```cpp
QString html = QString("<span style=\"color: #FFFFFF; font-family: 'Courier New', monospace;\">%1</span>  <span style=\"color: #FFD700;\">%2</span>")
                   .arg(timestamp, entry.content);
```

---

## 注意事项

1. **线程安全**：LogManager 的所有操作都在主线程（GUI 线程）中执行。如果日志来源于其他线程，需通过信号槽跨线程传递。
2. **行数限制**：QTextEdit 不像 QPlainTextEdit 有 `maximumBlockCount`，当前通过手动删除超出部分实现。如果日志量极大（>10万行），建议改用 QPlainTextEdit 并放弃颜色功能。
3. **时间戳**：当前取输出时刻的时间。如果需要记录日志产生时间，可在 LogEntry 中添加 `QDateTime timestamp` 字段。
4. **定时器启停**：队列为空时 QTimer 自动停止，有新日志入队时自动启动，无需手动管理。
